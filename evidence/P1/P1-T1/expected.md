# P1-T1 expected evidence

- Admin auth scaffold files exist for login/logout/me flow.
- Login handler includes invalid credential contract marker (`AUTH_INVALID_CREDENTIALS`).
- Me handler includes unauthorized contract marker (`AUTH_REQUIRED`).
- Evidence bundle includes happy and negative case files.
