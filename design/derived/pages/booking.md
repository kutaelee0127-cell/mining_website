# /booking — derived UI spec

Depends-on: `design/derived/states/app__layout-bento.md`

## Goal
- Strong booking CTA to Naver store.
- Admin can update booking link and copy.

## Layout
- Hero CTA card appears above the fold on mobile (360px).
- Primary action opens Naver booking link in new tab with safe rel.
- Secondary hint text uses `msg.externalLinkHint`.
